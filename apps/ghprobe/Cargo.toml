[package]
name = "ghprobe"
description = "Can this handset talk to api.github.com? One question, one binary."
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
symbian-sys = { path = "../../crates/symbian-sys" }
symbian = { path = "../../crates/symbian" }
symbian-app = { path = "../../crates/symbian-app" }
