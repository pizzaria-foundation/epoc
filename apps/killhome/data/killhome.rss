//  killhome UI resource. Caption for the menu; no custom icon (number_of_icons = 0), so the shell
//  draws its default — enough for an escape-hatch entry.
NAME KILL

#include <eikon.rh>
#include <avkon.rsg>
#include <avkon.rh>
#include <appinfo.rh>

RESOURCE RSS_SIGNATURE { }
RESOURCE TBUF { buf = ""; }
RESOURCE EIK_APP_INFO { }

RESOURCE LOCALISABLE_APP_INFO r_killhome_localisable_app_info
    {
    short_caption = "Kill Home";
    caption_and_icon =
        CAPTION_AND_ICON_INFO
            {
            caption = "Kill Home";
            number_of_icons = 0;
            };
    }
