[package]
name = "imgprobe"
description = "Asks the handset's JPEG codec which decode configuration actually works"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
symbian-ui = { path = "../../crates/symbian-ui" }
symbian-gfx = { path = "../../crates/symbian-gfx" }
symbian-sys = { path = "../../crates/symbian-sys" }
symbian = { path = "../../crates/symbian" }
