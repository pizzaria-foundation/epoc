NAME KPRB

#include <eikon.rh>
#include <appinfo.rh>

RESOURCE RSS_SIGNATURE { }
RESOURCE TBUF { buf = ""; }
RESOURCE EIK_APP_INFO { }

RESOURCE LOCALISABLE_APP_INFO r_keyprobe_localisable_app_info
    {
    short_caption = "KeyProbe";
    caption_and_icon =
        CAPTION_AND_ICON_INFO
            {
            caption = "Key Probe";
            /* No icon: the shell draws a default, and a diagnostic tool does not
             * need one. Saves the bmconv step entirely. */
            number_of_icons = 0;
            };
    }
