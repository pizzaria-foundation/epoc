[package]
name = "preview"
description = "Host-side renderer: draws SDK screens to PNG so they can be inspected without a device"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
symbian-gfx = { workspace = true }
symbian-ui = { workspace = true }
tg = { workspace = true }
